apiVersion: v1
kind: Service
metadata:
  name: {{ include "farm-dora-client.fullname" . }}
  labels:
    {{- include "farm-dora-client.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "farm-dora-client.selectorLabels" . | nindent 4 }}
